import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const HSMain = () => {
	return (
		<View>
			<Text>HSMain</Text>
		</View>
	);
};

export default HSMain;

const styles = StyleSheet.create({});
